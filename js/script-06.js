const num = Number( prompt("Enter a number") );

/*
 Javascript te === veya !== ifadelerinde hem değer hem tip kontrolü yapılır
 == veya != ifadelerinde ise sadece değer kontrolü yapılır.

 "0" !== 0   => true
 "0" != 0    => false

 "0" === 0   => false
 "0" == 0    => true
*/

if(num !== 0){
    const result = 50 / sayi;
    alert("Result: " + result);
}